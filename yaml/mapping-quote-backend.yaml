apiVersion: getambassador.io/v3alpha1
kind: Mapping
metadata:
  annotations: {}
  labels:
    argocd.argoproj.io/instance: ac-quote-default
  name: quote-backend
  namespace: demo
spec:
  ambassador_id:
  - default
  hostname: demo.kjames.k736.net
  labels:
    ambassador:
    - header_request_label:
      - request_headers:
          header_name: x-token-name
          key: headerkey
  prefix: /backend/
  service: quote.default:80
  timeout_ms: 3000
